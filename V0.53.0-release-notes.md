# gold-etf-analyzer V0.53.0 发布说明

> 提交 `b7da418` ｜ 标签 `v0.53.0`（main 已推送 GitHub；tag 因代理 502 待补推）｜ 服务运行：http://127.0.0.1:8888

## 一、本次落地：UX 6.2 响应式与移动端适配

在四个前端页面（趋势追踪 / 个人持仓 / 权重配置 / 消息面评估）上实现窄屏与移动端自适应，让手机、平板也能顺畅使用。

### 实现方式
- 新增**共享样式表** `static/responsive.css`，含三档媒体查询断点：
  - `≤768px`（平板/小屏笔记本）：收缩页面内边距、卡片内边距、顶栏内边距；
  - `≤480px`（手机）：顶栏链接换行、面板纵向堆叠、固定宽列收窄、表单与输入框占满整行、表格横向滚动、按钮触控放大；
  - `≤360px`（超小屏）：进一步收窄标签列、时效条字号微调。
- 四个页面的 `</style>` 之后注入 `<link rel="stylesheet" href="/static/responsive.css">`。由于链接样式表在文档顺序上位于内联 `<style>` 之后，**同特异性的媒体查询在窄屏时正确覆盖基准样式**。

### 具体覆盖点
| 场景 | 适配行为 |
|------|----------|
| 顶部导航 | 链接在窄屏自动换行、字号缩小，不再横向溢出 |
| 追踪指数面板 | 纵向堆叠，指数分数与解读上下排列 |
| 宏因子行 / 权重行 | 固定宽列（110/130px）收窄为 84/92px，避免挤压进度条 |
| 表单与输入框 | 手机上字段与输入框占满整行、纵向排列，便于单手填写 |
| 持仓表格 | 在 `#posArea` 容器内横向滚动，彻底解决「表格撑破页面」问题 |
| 按钮 / 芯片 | 最小高度提升到 40px，触控目标更友好 |
| 数据时效条 | 超小屏字号微调，保持可读 |
| 关键指标 | 评估指数 / 建议仓位 / 今日操作清单在窄屏首屏优先置顶，一屏可读 |

### 范围说明
- 本次**未做**「顶部导航折叠为抽屉菜单」：这是更高阶的优化，当前链接已可换行容纳，暂够用，列入后续可选增强。
- 基础能力（viewport 声明、CSS 变量主题、卡片 `auto-fit` 栅格）此前已具备，本次补齐的是断点、固定宽列、表格滚动与触控目标。

## 二、版本与文档
- `pyproject.toml` 与 `src/app/main.py` 版本 `0.52.0 → 0.53.0`
- `docs/improvement-path.md`：6.2 标记 ✅ 已落地（V0.53.0）；版本路线表新增 V0.53.0 行；验收度量「当前」更新为 V0.53.0
- `docs/application-guide.md`：版本历史新增 V0.53.0 条目

## 三、验证
- 运行服务上：`/static/responsive.css` 返回 200；四页均含 `responsive.css` 引用（各 1 处）；样式表含 3 个 `@media` 断点；所用选择器（`.topnav`/`.index-panel`/`.macro-row`/`.w-row`/`#posArea`/`table`/`button`/`.fresh-bar` 等）均与页面真实类名匹配。
- 纯静态改动（无 Python 逻辑变更），服务从磁盘直接提供，无需重启即生效。

## 四、GitHub 同步状态
- 主分支：`ff32725..b7da418` 已推送 ✅
- 标签 `v0.53.0`：因沙箱代理隧道瞬时 502 未能推送，待网络恢复后执行 `git push origin v0.53.0` 即可（本地标签已创建并指向 `b7da418`）。
