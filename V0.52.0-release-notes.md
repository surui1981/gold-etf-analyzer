# gold-etf-analyzer V0.52.0 发布说明

> 提交 `ff32725` ｜ 标签 `v0.52.0` ｜ 已推送 GitHub（`surui1981/gold-etf-analyzer`）
> 服务运行：http://127.0.0.1:8888 （PID 19208，启动约 3s，Alembic 子进程化不再死锁）

## 一、本次完成的三件事

### 1. UX 6.1 数据时效透明（已落地并验证）
- **三市场时段判定**（`utils/market_clock.py`）：纽约金 CME Globex（近乎全天+17:00-18:00 ET 结算间歇）、上海金 SGE（日盘+夜盘）、黄金 ETF（A 股交易时段）+ 时效分级 `realtime / delayed / t1 / stale / mock`。
- **时效服务与端点**：`services/freshness.py`（FreshnessService）+ `GET /api/v1/market/freshness`（返回 server_time / degraded / 各市场 session+freshness+数据截止日）。
- **趋势内嵌时效**：`GET /api/v1/market/gold/trend?target=ny` 响应中直接带 `freshness` 字段。
- **全站时效条**（前端 `static/freshness.js`）：顶部常驻「时段 + 时效等级 + 数据截止日 + 采集时间」，60s 刷新；趋势页主面板/上海金面板对 stale/mock 持续角标，绝不静默。

### 2. 集成远程「零KEY公开数据源」重构
远程在 `e5f3c47` 之后已推进两笔重构：`98833f9`（接入零KEY公开源、移除付费API依赖）、`226b6e7`（新浪 hf_GC 作 XAU/USD 兜底）。本版本在重构之上 rebase 并手工并入 6.1 的 `market_data` 采集元信息钩子（`source_meta` / `_mark(last_date=...)`），实测 NY 金已成功拉取真实数据（`status=live, date=2026-09-04`）。

### 3. 修复重构遗留的破坏性 bug
远程重构删除了 `market_data.health_stats()`，但 `market.py` 的 `/market/health` 仍 import 它 → 应用无法 import。已改为使用重构后的实例级 `source_status()`，应用恢复正常导入（`/market/health` 现已可访问）。

## 二、启动健壮性（延续 V0.51.1 修复）
Alembic 迁移改为**独立子进程**执行（`subprocess.run(timeout=60)`，OS 级超时 kill 释放 SQLite 写锁），彻底消除此前「应用内调用 Alembic 异步 env 在事件循环内死锁导致启动 hang」的问题。启动日志：`alembic upgrade head: ok` → database ready → ny/etf/gram 三市场预热完成。

## 三、版本与文档
- `pyproject.toml` 与 `main.py` 版本 `0.51.1 → 0.52.0`
- `docs/improvement-path.md`：6.1 标记已完成；版本路线表新增 V0.52.0 行
- `docs/application-guide.md`：版本历史新增 V0.52.0 条目
- `.gitignore`：新增 `server_run.log`

## 四、测试与验证
- 49 项 6.1 相关测试通过（`test_market_clock` / `test_freshness` / `test_market`）
- 运行态核验：`/market/freshness` 正确分级（纽约金 实时/live、上海金·ETF T-1/live）；`/market/health` 正常返回；全站时效条可加载。

## 五、已知小限制（非阻塞）
`/market/health` 的 `sources` 为空 —— 远程重构后 repository 为 per-request 实例，`source_status()` 无跨请求累计（功能正常、不崩溃，仅当次请求无历史）。如需长期健康度统计，建议把数据源状态改为进程级单例。

## 六、GitHub 同步状态（终验）
- `refs/heads/main` → `ff32725` ✓
- `refs/tags/v0.52.0`（附注标签）→ 解引用 → `ff32725` ✓
- 本地 HEAD / 标签 / 远程 三者一致 ✓
